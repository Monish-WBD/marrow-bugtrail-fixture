import Foundation

/// Prints the banner shown in the console when a playback session starts.
public enum PlaybackGreeting {
    public static func announceSessionStart() {
        print("Welcome")
    }
}
